{
  "buildCommand": "npm --prefix client install && npm --prefix client run build",
  "functions": {
    "api/index.js": {
      "memory": 300,
      "maxDuration": 30,
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
